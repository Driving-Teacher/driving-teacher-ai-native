# 필기파헤치기 시스템 스펙 (Layer 2)

> 최종 업데이트: 2026-06-02
> 성격: **구현 스펙** 문서 (콘텐츠/브랜드 전략은 `channel-bible.md`의 [필기파헤치기] 섹션 참조)
> 진리원본: 이 문서가 필기파헤치기 시스템 설계의 단일 진리원본(canonical). 메모리 파일은 트리거·재개 포인터 역할만 한다.
> 재개 트리거: **"필기파헤치기 시스템 이어서"**

---

## 0. 이 문서의 위치

필기파헤치기 결정은 두 종류로 나뉜다.

| 종류 | 저장 위치 |
|------|-----------|
| 콘텐츠/전략 (영상 길이, CTA 방향, 포맷 구조, 비주얼 컨셉, 업로드 주기, 멘트 톤) | `channel-bible.md` [필기파헤치기] 섹션 |
| 구현 스펙 (입력 계약, Firestore 필드, Storage/Mux 경로, 다운로드 로직, 슬래시 스킬, Ep 카운터) | **이 문서** |

이전에는 설계 결정이 메모리 파일 한 곳에만 있었고, 세션이 중간에 끊기면 그 세션의 발견이 통째로 증발하는 문제가 있었다(실제로 2026-05-27 세션의 C 발견이 그렇게 날아갔다가 2026-06-02에 코드베이스에서 복구됨). 그래서 레포 안 committed 문서로 영속화한다.

---

## 1. 프로젝트 개요

운전선생 필기 약 1000문제 → 30~50초 Shorts 영상으로 자동 스크립트화하는 시스템.

- **Layer 1 (PM 완료, 우리가 손대지 않음)**: 풀해설을 `Problems2026V2.ai.summary`에 1000개 전부 적재 완료.
- **Layer 2 (우리 설계 영역)**: `ai.summary`를 받아 자유 산문 대본(대사만) 생성 + AI 일타강사 영상용 시각 자산 자동 다운로드. 모델 Sonnet 4.6.

**Why 새로 만드나**: 기존 AI 해설(`base.choices[].explanation`)이 별로라서 PM이 새 버전(`ai.summary`)을 만들었고, 우리는 그걸 기반으로 영상을 찍는다.

**Why 영상화인가**: 유튜브 채널 빠른 성장이 1순위, 운전선생 서비스 유입이 궁극 목표(2순위).

---

## 2. 입력 계약 — `Problems2026V2` 한 doc 기준

| 필드 | 용도 | 등급 |
|---|---|---|
| `ai.summary` | 영상 스크립트 원천, **단일 진리원본** (PM 작업) | 🔴 핵심 |
| `base.problem` | 문제 본문 (대사·자막에 직접 인용 가능) | 🔴 핵심 |
| `base.choices[].text` | 보기 텍스트 (정답·오답 함정 짚기) | 🔴 핵심 |
| `answer` | 정답 인덱스 **배열 `number[]`** (복수정답 가능 — 단수 아님) | 🔴 핵심 |
| `media.problem[]` | 이미지/영상 URL (자동 다운로드) | 🟡 있을 때만 |
| `base.choices[].explanation` | 레거시 AI 해설 — **사용 금지** | ❌ 금지 |
| `base.explanation` | 공단 원본 — 직접 인용 X, 사실·법조 검증 참고만 | 🟢 보조 |
| `type` | 문장형/사진형/안전표지형/일러스트형/동영상형 — 분기 X, 메타데이터 | ⚪ 메타 |
| `id`, `index` | 식별·폴더명 | ⚪ 메타 |
| `translations` | 다국어 (v1 미사용, v2) | ⚫ 미사용 |
| `media.choices/answer` | 거의 비어있음 | ⚫ 미사용 |

---

## 3. Firestore 검증 사실 (2026-05-22 직접 read)

- 컬렉션: `Problems2026V2` (production `drivingteacher-eeb82`)
- 총 도큐먼트: **1000개**
- `ai.summary` 커버리지: **1000/1000 (100%)**, 길이 min 415 / median 581 / max 650자 (분량 이슈 종결)

### 문제 type 분포
| type | 개수 | 비주얼 처리 |
|---|---:|---|
| 문장형 | 680 | AI 일러스트 (자체 미디어 없음) |
| 사진형 | 100 | 공단 원본 (`media.problem[]`) |
| 안전표지형 | 100 | 공단 원본 (`media.problem[]`) |
| 일러스트형 | 85 | 공단 원본 (`media.problem[]`) |
| 동영상형 | 35 | 공단 원본 (Mux — 4절 참조) |

- `media.problem[]` 보유: 285개 (사진형 100 + 안전표지형 100 + 일러스트형 85)
- 공단 원본 영상 사용 권한 OK (2026-05-22 giheung 확인)

### Firestore 접근 (참고만 — 직접 read/write 엄금, 혼선 방지)
- 서비스 계정 키: `~/Documents/GitHub/driving-teacher-frontend/packages/cloud-functions/drivingteacher-eeb82-firebase-adminsdk-sas55-a951c5fde6.json`
- `firebase-admin@13.2.0` cloud-functions에 설치됨

---

## 4. ✅ C 해결 — 동영상형 35개 경로 = Mux (2026-06-02 코드베이스 검증 복구)

**2026-05-27 세션에서 조사했으나 세션이 끊겨 메모리에 저장 안 됐던 발견. 2026-06-02 코드베이스에서 재검증 완료.**

- 동영상형 35개 = 문제 ID **966~1000** (정확히 35개, `1000-966+1=35`)
- 원본 MP4는 **Firebase Storage에 없음** → **Mux**에 있다 (next-video가 빌드 시 Mux 업로드)
- 로컬 메타데이터: `~/Documents/GitHub/driving-teacher-frontend/apps/web/videos/Q{id}.mp4.json` (35개 파일 실재 확인)
- 각 JSON에 `providerMetadata.mux.playbackId` 포함
- **재생 URL**: `https://stream.mux.com/{playbackId}.m3u8` (HLS)
- **썸네일/포스터**: `https://image.mux.com/{playbackId}/thumbnail.webp`
- 예시: Q978 → playbackId `mYvmppMlKcpDjdM6W400AZ3nHiPAbHdsWzRtDqyuhCTc`

`.mp4.json` 실제 구조:
```json
{"status":"ready","originalFilePath":"videos/Q978.mp4","provider":"mux",
 "providerMetadata":{"mux":{"playbackId":"mYvmppMlKcpDjdM6W400AZ3nHiPAbHdsWzRtDqyuhCTc", ...}},
 "sources":[{"src":"https://stream.mux.com/{playbackId}.m3u8","type":"application/x-mpegURL"}],
 "poster":"https://image.mux.com/{playbackId}/thumbnail.webp"}
```

> ⚠️ Mux는 HLS 스트림이라 단일 mp4 파일 다운로드와 다르다. 영상 자산을 로컬 파일로 받으려면 `ffmpeg`로 m3u8 → mp4 변환이 필요할 수 있음 (L 결정 시 확정).

---

## 5. 락인 19개 (2026-05-26 세션 확정)

### 목적·구조 (1~7)
1. **목적 우선순위** = 채널 성장 1순위 / 서비스 유입 2순위(궁극 목표)
2. **영상 단위** = 1영상 = 1문제
3. **영상 길이** = 30~50초 (Shorts 알고리즘 최적)
4. **본편 카탈로그 매핑** = v1 폐기 (v2 재검토 가능)
5. **해설 정보량** = 풀해설(`ai.summary`)보다 압축, 단 **핵심 누락 절대 금지**
6. **오프닝·클로징 패턴** (2026-06-02 I 확정으로 개정) = **오프닝은 정형 브랜드 인사 안 씀** — 강력한 후크 1문장으로 시작(아래 9절). 클로징만 정형(슬로건+CTA). 브랜드 인지는 비주얼(일타강사 캐릭터·Layer B 카드)과 클로징 슬로건이 담당.
7. **CTA** = "다음 영상 후크 + 댓글 유도" 하이브리드. **본편 깔때기 X, 앱 깔때기 X.** retention·engagement 집중

### 입력·출력 (8~9, 15~16)
8. **입력 계약** = `ai.summary` 단일 진리원본 / `base.choices[].explanation` 사용 금지 / `base.explanation` 검증 참고만 (2절 참조)
9. **출력 sub①** = **대사만** 마크다운. 자막·시각 큐·사운드 큐 X. 편집은 giheung 영역
15. **D 출력 형식** = **자유 산문** (단별 섹션·표·yaml-ish X). → E(타이밍)·G(단 수)·H(시간 비중) 자동 해소
16. **M 분량** = Sonnet 자율 (글자수 비명시), 30~50초에 맞게만

### 비주얼 (10~14)
10. **Main 비주얼 = AI 일타강사 인물모델** (제작됨, 채널 main 캐릭터). 일타강사가 verbal로 끌고 가고 나머지 시각 자산은 보조
11. **공단 원본 영상 사용 권한 OK** (`media.problem[]` 285개 + 동영상 Mux)
12. **동영상형 35개** = Mux 경로로 확정 (4절, ✅ C 해결됨)
13. **비주얼 3겹 구조**: Layer A(AI 일타강사, primary) / Layer B(문제·보기 텍스트 카드, 전 영상 overlay) / Layer C(보조 비주얼 — 공단 원본 or AI 일러스트, 케이스별)
14. **Layer C 판단 = Sonnet 자동 이분법** (selective X, 단순 if/else):
    ```
    if media.problem.length > 0 OR 동영상형(966~1000):
        use 공단 원본   # 320개 (285 + 35)
    else:
        generate AI 일러스트   # 680개 (문장형)
    ```

### 운영 (17~21)
17. **F 출력 flow = 단계별** (드래프트 → giheung 코멘트 → 다듬기, 핑퐁 1~N회)
18. **J 문제 추천 = 완전 랜덤 1개** (옛 "후보 3개 → 1픽" 폐기)
19. **K 회차 카운터 = `Ep{N}`** (영문), N = 대본 만든 수 누적(발행 수 아님)
    - 폴더 패턴: `~/Downloads/필기파헤치기/{날짜}-Ep{N}-{problemId}/`
    - 카운터: 별도 상태 파일 X. `~/Downloads/필기파헤치기/` 하위 `Ep*` 디렉터리 수 + 1
20. **N 슬래시 스킬 호출 방식 = 완전 랜덤만** (2026-06-02 확정). `/필기파헤치기` 호출 시 1000개 중 랜덤 1개 자동 선택. 문제 ID 지정 옵션·후보 제시 없음. 가장 단순. (락인 #18과 정합)
21. **O 핑퐁 방식 = 자유 코멘트형** (2026-06-02 확정). 스킬이 대본 드래프트 1개를 통째로 제시 → giheung이 자유 코멘트 → 반영해서 다시 제시 → 만족할 때까지 1~N회. 스킬이 구조 잡아 되묻거나 단계 쪼개지 않음.

---

## 6. 미결정

| # | 항목 | 상태 |
|---|------|------|
| ~~N~~ | 슬래시 스킬 호출 방식 | ✅ 완전 랜덤만 (락인 #20) |
| ~~O~~ | 핑퐁 방식 | ✅ 자유 코멘트형 (락인 #21) |
| ~~C~~ | 동영상형 경로 | ✅ Mux (4절) |
| ~~L~~ | 자산 자동 다운로드 로직 | ✅ 확정 (아래 락인 #22) |
| ~~I~~ | 캐릭터 컨셉 + 멘트 패턴 | ✅ 확정 (아래 9절) |

**→ 미결정 5개 전부 해결됨 (2026-06-02). 남은 건 `/필기파헤치기` 슬래시 스킬 구현뿐.**

## 5-b. 락인 22 — L 자산 자동 다운로드 로직 (2026-06-02 확정)

스킬은 선택된 문제 1개에 대해 Ep 폴더 `~/Downloads/필기파헤치기/{날짜}-Ep{N}-{problemId}/`에 대본 + 자산을 모은다. 비주얼 분기는 락인 #14 이분법을 따른다.

| 대상 | 개수 | 다운로드 방식 | 도구 |
|------|---:|------|------|
| 공단 이미지 (사진형·안전표지형·일러스트형) | 285 | `media.problem[]` URL → `curl` 다운로드 | curl ✅ |
| 동영상형 (ID 966~1000, Mux) | 35 | `Q{id}.mp4.json`의 playbackId → ① `ffmpeg`로 `stream.mux.com/{id}.m3u8` → **mp4 변환** ② `image.mux.com/{id}/thumbnail.webp` 포스터 **둘 다** 저장 | ffmpeg ✅ (v8.1) |
| 문장형 AI 일러스트 | 680 | **자동 생성 불가** (이미지 생성 도구 미설치) → 스킬은 **일러스트 생성 프롬프트만** 텍스트로 제공. 실제 생성·편집은 giheung이 외부 도구로 | (도구 깔면 확장) |

> 제약: 2026-06-02 기준 gemini CLI·이미지 생성 MCP 없음. 680개 일러스트는 프롬프트 산출까지가 자동화 한계. 추후 이미지 생성 도구 연결 시 L 재설계.

---

**추천 다음 순서**: I(캐릭터 컨셉 → 멘트 패턴) 확정 → `/필기파헤치기` 슬래시 스킬 구현 (대본 생성 + L 다운로드 로직 코드화).

미진행 상태: `/필기파헤치기` 슬래시 스킬 ❌, 실제 Ep 대본 ❌.

---

## 7. 관련 산출물

GitHub repo: https://github.com/Driving-Teacher/driving-teacher-ai-native

- `필기파헤치기-Layer1-해설-작성-지침서.md` (committed, **historical** — PM이 별도 매뉴얼로 작업)
- `필기파헤치기-자동화-브리프.md` (로컬만, historical)
- `필기파헤치기-Layer1-예시-3편.md` (로컬만, 톤·구조 참고용 유효. 단 분량 락인 폐기 반영)
- 옛 다운로드 샘플(Day 표기): `~/Downloads/필기파헤치기/2026-05-11-Day1-689/` — 신규 폴더는 Ep 표기

---

## 8. 검토 보류 (재개 시 확인)

- 영상 공개 채널이 기존 유튜브 채널인지 신규인지 — Ep1 시작점 정의에 영향
- 영상 외곽(채널 description / 핀 댓글 / 카드)의 서비스 유입 장치 톤·문구
- main 브랜치 직접 push 정책 — PR 권장일 수 있음

---

## 9. AI 일타강사 캐릭터 컨셉 + 멘트 패턴 (I 확정, 2026-06-02)

> 인물모델 원본 이미지: `/Users/giheung/Desktop/캠페인/DT/11.유튜브/모델/5.png` (레포 밖). 20대 후반~30대 초반 남성, 따뜻한 미소, 베이지 카디건+흰 티, 아늑한 서재. "부담 없는데 믿음직한 옆자리 선배" 인상.

### 이름
**= 운전선생.** 채널 브랜드 자체가 캐릭터. 별도 캐릭터명 안 만든다.

### 성격
- 안심시키는 타입 — "그거 헷갈리는 거 당연해요" 먼저 공감
- 똑똑한데 안 나서는 — 원리를 쉽게 풀어주되 자랑 안 함
- 차분·따뜻, 절대 겁주거나 다그치지 않음 (channel-bible Don't 준수)

### 말투
- 존댓말 기본 (반말 금지)
- "~인데요", "~거든요", "~해볼게요" 부드러운 어미
- 원리 먼저: "이거 왜 정답이냐면요—"
- 가벼운 공감 한 스푼: "이 문제, 진짜 많이 틀려요"

### 오프닝 (정형 인사 ❌ — 후크 우선)
- **"운전선생입니다" 류 브랜드 인사로 시작 금지.** 첫 1초에 브랜드 박으면 Shorts 이탈.
- **강력한 후크 1문장으로 연다.** 문제별 변주, 패턴은 충격 사실 / 공감 / 도발 질문 등.
  - 예: "이 표지, 10명 중 8명이 틀립니다." / "이거 모르고 시험 보면 그냥 떨어져요." / "이 두 개 헷갈렸다면, 이유가 있어요."
- 브랜드 인지는 **비주얼(일타강사 캐릭터·Layer B 카드) + 클로징 슬로건**이 담당.

### 클로징 (정형 — 잠정 확정, 계속 디벨롭)
> "제대로 알면, 운전은 즐겁죠. 다음 편은 [다음 문제 후크]—헷갈린 거 있으면 댓글로 남겨주세요."

- 슬로건("제대로 알면, 운전은 즐겁다") 자연스럽게 녹임 → 브랜드 클로징 통일 (락인 #6)
- 다음 영상 후크 + 댓글 유도 = 락인 #7 정합. **본편/앱 깔때기 없음.**

> 상태: 오프닝 규칙·클로징 문구 모두 "잠정 확정". 실제 대본 뽑으면서 계속 다듬는다.
