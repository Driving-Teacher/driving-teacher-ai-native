# AI Native Camp 꿀팁 모음

> 팀원들이 발견한 유용한 팁을 모아두는 곳입니다.
> 슬랙에서 봇한테 "꿀팁" 물어보거나, Claude Code에서 `/tips` 치면 볼 수 있어요.

---

## CLAUDE.md 팁

- "표로 정리해줘"를 CLAUDE.md에 넣으면 결과가 훨씬 깔끔해짐
- "한국어로 대화해"는 기본으로 넣어두세요

## MCP 팁

- MCP 연결할 때 **개인 계정으로 먼저** 해보세요. 회사 계정은 Admin 권한에 막힐 수 있어요.
- API 키는 대화창에 직접 입력하지 마세요 — `claude mcp add` 명령어로 안전하게.

## 스킬 팁

- 스킬은 `.claude/skills/스킬이름/SKILL.md` 구조
- "이거 자동화해줘"라고 시키면 Claude가 알아서 스킬 파일을 만들어줌

## CLAUDE.md 꿀팁

- CLAUDE.md = Claude에게 주는 매뉴얼. **매 세션 시작 시 자동으로 읽힘**
- 200줄 넘기지 마세요 — 길면 Claude가 뒷부분을 무시할 수 있음
- **넣어야 할 것**: 프로젝트 설명, 기술 스택, 빌드/테스트 명령어, 코드 스타일 규칙
- **넣지 말아야 할 것**: 임시 할일, 일정, 긴 절차 (→ 스킬이나 별도 파일로)
- Claude가 코드에서 알 수 있는 건 안 넣어도 됨 — **Claude가 모를 것만** 넣기
- 폴더별 CLAUDE.md 가능 — `src/api/CLAUDE.md`에 API 규칙, `src/frontend/CLAUDE.md`에 프론트 규칙
- 중요한 규칙은 `<important>` 태그로 감싸면 무시 안 함
- `@docs/git-instructions.md` 처럼 다른 파일 참조 가능 — 본문에 다 넣지 말고 참조로

## 스킬 작성 꿀팁

- **description이 제일 중요** — Claude는 이름+설명만 보고 스킬을 로드할지 결정함. 설명이 부실하면 스킬이 절대 실행 안 됨
- "왜"를 설명하면 "무엇"만 말하는 것보다 결과가 좋음 — Claude는 이유를 알면 더 잘 일반화함
- 예시를 넣으면 품질이 확 올라감 — 입력 예시 + 출력 예시
- 하나의 스킬 = 하나의 업무. 여러 업무를 합치지 마세요
- SKILL.md는 짧게 (50줄 이내), 자세한 건 `references/` 폴더에
- 만든 후 Claude에게 "이 스킬 읽고 모호한 부분 알려줘" 시키면 개선점이 나옴 (역 프롬프팅)
- **3번 같은 걸 설명했으면 → 그건 스킬로 만들어야 할 것**

## 실전 워크플로우 팁

- **"fix"만 치세요** — 에러 붙여넣고 "fix" 한 마디면 80%는 고쳐짐. 과도한 설명은 오히려 방해
- **Plan Mode** — 복잡한 작업 전에 `Shift+Tab` 두 번 → 계획만 세우고 코드는 안 씀. 계획 확인 후 실행
- **컨텍스트 60% 넘기지 마세요** — 대화가 길어지면 `/compact` 또는 `/clear`로 정리
- 막히면 2번만 시도 → 안 되면 `/clear`하고 새로 시작
- 파일 참조: "이 파일 읽고 따라해" > 내용을 복붙해서 설명

## 터미널 팁

- 줄바꿈: macOS `Option+Enter` / Windows `Alt+Enter`
- Windows는 CMD 말고 **Windows Terminal** 쓰세요
- 폰트는 **Cascadia Code** 추천
